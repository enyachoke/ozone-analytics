{
  "jdbcDriverClass" : "org.apache.hive.jdbc.HiveDriver",
  "databaseService" : "hive2",
  "databaseHostName" : "${THRIFT_SERVER_HOST}",
  "databasePort" : "${THRIFT_SERVER_PORT}",
  "databaseUser" : "${THRIFT_SERVER_USER}",
  "databasePassword" : "${THRIFT_SERVER_PASSWORD}",
  "databaseName" : "${THRIFT_SERVER_DB}"
}