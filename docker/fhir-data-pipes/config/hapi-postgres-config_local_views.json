{
  "jdbcDriverClass": "org.postgresql.Driver",
  "databaseService" : "postgresql",
  "databaseHostName" : "${ANALYTICS_DB_HOST}",
  "databasePort" : "${ANALYTICS_DB_PORT}",
  "databaseUser" : "${ANALYTICS_DB_USER}",
  "databasePassword" : "${ANALYTICS_DB_PASSWORD}",
  "databaseName" : "${ANALYTICS_DB_NAME}"
}
